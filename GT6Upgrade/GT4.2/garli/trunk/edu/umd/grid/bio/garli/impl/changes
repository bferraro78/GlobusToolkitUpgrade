203,204d202
< 	int searchreps = -1;
< 	int bootstrapreps -1;
264a263,264
> 
> 
266c266,267
< 	    if(ratematrix == null || (!ratematrix.equals("6rate") && !ratematrix.equals("2rate") && !ratematrix.equals("fixed") && !ratematrix.equals("jones") && !ratematrix.equals("dayhoff"))) { // either not supplied or we haven't seen it before... use default value
---
> 	    ArrayList<String> distinctRateMatrix = GSBLUtils.returnDistinctValues("garli", "ratematrix");
> 	    if(ratematrix == null || (!distinctRateMatrix.contains(ratematrix))) { // either not supplied or we haven't seen it before... use default value
277c278,279
< 	    if(statefrequencies == null || (!statefrequencies.equals("estimate") && !statefrequencies.equals("empirical") && !statefrequencies.equals("fixed") && !statefrequencies.equals("f3x4"))) { // either not supplied or we haven't seen it before... use default value
---
> 	    ArrayList<String> distinctStateFrequencies = GSBLUtils.returnDistinctValues("garli", "statefrequencies");
> 	    if(statefrequencies == null || (!distinctStateFrequencies.contains(statefrequencies))) { // either not supplied or we haven't seen it before... use default value
331,332c333,334
< 	    searchreps = Integer.valueOf(gp.getSearchreps());
< 	    bootstrapreps = Integer.valueOf(gp.getBootstrapreplicates());
---
> 	    int searchreps = Integer.valueOf(gp.getSearchreps());
> 	    int bootstrapreps = Integer.valueOf(gp.getBootstrapreplicates());
407c409
< 	    addToDB(myBean.getOwner(), myBean.getAppName(), myBean.getJobName(), myWorkingDir, argumentString, scheduler, resource, arch_os, job.getCPUs(), job.getReplicates(), (new Integer(runtime_estimate_seconds).toString()), (new Integer(searchreps).toString()), (new Integer(bootstrapreps).toString()));
---
> 	    addToDB(myBean.getOwner(), myBean.getAppName(), myBean.getJobName(), myWorkingDir, argumentString, scheduler, resource, arch_os, job.getCPUs(), job.getReplicates());
